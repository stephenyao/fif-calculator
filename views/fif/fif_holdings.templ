package fif

import "fmt"
import . "fif-calculator/internal/model"
import . "fif-calculator/views"

templ RenderHoldingDetail(holding *HoldingInfo, year int) {
	<form
		hx-post="/fif/calculate"
		hx-target="#fif-result"
		hx-swap="innerHTML"
		class="space-y-6"
		id="fif-form"
	>
        @HoldingPanel(holding, year)
	</form>
}


templ HoldingsInputForm(holdings []*HoldingInfo, year int) {
	@InitializeClientSideScripts(holdings)
	@Section() {
		<div id="holdings-segment"class="flex flex-wrap gap-2">
			for _, h := range holdings {
				<button
					type="button"
					class="px-3 py-1.5 text-sm rounded-md border transition-colors focus:outline-none focus:ring-2 focus:ring-blue-500/30 dark:focus:ring-blue-400/30 bg-white text-gray-800 border-gray-300 hover:bg-gray-50 dark:bg-gray-800 dark:text-gray-100 dark:border-gray-700 dark:hover:bg-gray-700"
					data-symbol={h.Symbol}
					hx-get={h.GetHoldingFIFInfoURL()}
					hx-target="#holding-panel"
					hx-swap="innerHTML"
				>
					{h.Symbol}
				</button>
			}
		</div>
	}
	@Section() {
	    @RenderHoldingDetail(holdings[0], year)
	}
	@Section() {
        <button
            type="button"
            hx-post="/fif/calculate"
            hx-vals="js:{...window.buildFIFSubmission()}"
            hx-include="#fif-form"
            name="action"
            value="calculate"
            class="px-6 py-2 bg-blue-600 text-white font-medium rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition-colors
                       dark:bg-blue-500 dark:hover:bg-blue-400 dark:focus:ring-blue-400 dark:focus:ring-offset-gray-900"
        >
            Calculate FIF
        </button>
	}
}

templ InitializeClientSideScripts(data []*HoldingInfo) {
	@Scripts()
}

templ Scripts() {
   <script>
   (function () {
     if (window.__fifInit) return;
     window.__fifInit = true;

     // global cache of values keyed by input id, e.g. price_start_AAPL
     window.fifState = window.fifState || {};

     // Debug so you can see the wiring happened
     console.log("[FIF] input capture initialized");

     // Only fire when the event comes from within the current form
     function inForm(target) {
       const form = document.getElementById('fif-form');
       return form && form.contains(target);
     }

     // 1) Capture on committed change (blur or enter)
     document.addEventListener('change', (e) => {
       const el = e.target;
       if (!inForm(el)) return;
       if (!(el instanceof HTMLInputElement)) return;
       if (el.type !== 'number') return;
       if (!el.id) return;

       window.fifState[el.id] = el.value;
       // console.log('[FIF] change:', el.id, '=', el.value);
     });

     // 2) (Optional) Also capture in-progress typing
     // document.addEventListener('input', (e) => {
     //   const el = e.target;
     //   if (!inForm(el)) return;
     //   if (!(el instanceof HTMLInputElement)) return;
     //   if (el.type !== 'number' || !el.id) return;
     //   window.fifState[el.id] = el.value;
     // });

     // 3) Hydrate inputs inside #holding-panel after HTMX swaps
     document.body.addEventListener('htmx:afterSwap', (e) => {
       if (e.target && e.target.id === 'holding-panel') {
         e.target.querySelectorAll('input[type="number"][id]').forEach((el) => {
           if (el.id in window.fifState) el.value = window.fifState[el.id];
         });
         // console.log('[FIF] hydrated panel from fifState');
       }
     });

     // 4) (Optional) Snapshot current panel before a request in case user hasn't blurred
     document.body.addEventListener('htmx:beforeRequest', () => {
       const panel = document.getElementById('holding-panel');
       if (!panel) return;
       panel.querySelectorAll('input[type="number"][id]').forEach((el) => {
         window.fifState[el.id] = el.value;
       });
       // console.log('[FIF] snapshot before request');
     });

     // 5) Your HTMX POST will include these values:
     window.buildFIFSubmission = function () {
       return window.fifState; // keys like price_start_AAPL, etc.
     };
   })();
   </script>
}

templ HoldingPanel(h *HoldingInfo, year int) {
	<div id="holding-panel">
		<div>
			<div class="mb-4">
				@H4(h.Symbol)
				<div class="text-sm text-gray-600 mt-1 dark:text-gray-400">
					<span>Qty on 1 Apr { year - 1 }: { h.QuantityStart }</span>
					<span class="mx-4">•</span>
					<span>Qty on 31 Mar { year }: { h.QuantityEnd }</span>
				</div>
				if h.NumberOfTrades > 0 {
					<div class="text-sm text-gray-600 mt-1 dark:text-gray-400">
						There were a total of { h.NumberOfTrades } trades during the period
					</div>
				}
			</div>

			<input type="hidden" name={ "symbol_" + h.Symbol } value={ h.Symbol }/>
			<input type="hidden" name={ "quantity_start_" + h.Symbol } value={ fmt.Sprintf("%.2f", h.QuantityStart) }/>
			<input type="hidden" name={ "quantity_end_" + h.Symbol } value={ fmt.Sprintf("%.2f", h.QuantityEnd) }/>
			<input type="hidden" name="financialYear" value={ year }/>

			<!-- Price Information -->
			<div class="mb-6 flex flex-col space-y-3">
				@H5("Price Information")
				@grid() {
					@input(h.Symbol, "Opening Price (NZD)", "price_start", "price_start", fmt.Sprintf("%.2f", h.OpeningPrice), true)
					@input(h.Symbol, "Closing Price (NZD)", "price_end", "price_end", fmt.Sprintf("%.2f", h.ClosingPrice), true)
				}
			</div>

			<!-- Income -->
			<div class="mb-6 flex flex-col space-y-3">
				@H5("Income")
				@grid() {
					@input(h.Symbol, "Proceeds from sales", "proceeds_from_sales", "proceeds_from_sales", fmt.Sprintf("%.2f", h.ProceedsFromSales), true)
					@input(h.Symbol, "Dividends (NZD)", "dividends", "dividends", fmt.Sprintf("%.2f", h.GainLoss.Dividends), true)
					@input(h.Symbol, "Tax Credits (NZD)", "tax_credits", "tax_credits", fmt.Sprintf("%.2f", h.GainLoss.TaxCredits), true)
					@input(h.Symbol, "Other Gains (NZD)", "other_gains", "other_gains", fmt.Sprintf("%.2f", h.GainLoss.OtherGains), true)
				}
			</div>

			<!-- Costs -->
			<div class="mb-6 flex flex-col space-y-3">
				@H5("Costs")
				@grid() {
					@input(h.Symbol, "Cost of purchases", "cost_of_purchases", "cost_of_purchases", fmt.Sprintf("%.2f", h.CostOfPurchases), true)
					@input(h.Symbol, "Foreign Income Tax (NZD)", "foreign_income_tax", "foreign_income_tax", fmt.Sprintf("%.2f", h.GainLoss.ForeignIncomeTax), true)
					@input(h.Symbol, "Other Costs (NZD)", "other_costs", "other_costs", fmt.Sprintf("%.2f", h.GainLoss.OtherCosts), true)
				}
			</div>
		</div>
	</div>
}


templ NoFIFApplicable() {
	<div class="text-gray-900 dark:text-gray-100">FIF not applicable</div>
}

templ grid() {
	<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-4">
		{ children... }
	</div>
}

templ input(symbol, label, id, name string, value string, enabled bool) {
	<div>
		<label
			class="block text-sm font-medium text-gray-700 mb-1 dark:text-gray-300"
			for={ id + "_" + symbol }
		>
			{ label }
		</label>
			<input
				type="number"
				step="0.01"
				min="0"
				id={ id + "_" + symbol }
				name={ name + "_" + symbol }
				class="block w-full px-3 py-2 border border-gray-300 rounded-md
				       bg-white text-gray-900 placeholder:text-gray-400
				       focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500
				       dark:border-gray-700 dark:bg-gray-800 dark:text-gray-100 dark:placeholder:text-gray-500
				       dark:focus:ring-blue-400 dark:focus:border-blue-400"
				placeholder="5.00"
				value={ value }
			/>
	</div>
}
