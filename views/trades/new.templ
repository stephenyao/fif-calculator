package trades

import . "fif-calculator/views"

templ NewTradeForm(currentPath string) {
    @Layout("New Trade", currentPath) {
        @Page() {
            @Section() {
                @TradeForm(nil, "/trades")
            }
        }
    }
}